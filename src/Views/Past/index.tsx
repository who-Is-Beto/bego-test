import React from 'react';
import OrderTemplate from '../OrderTemplate';

const Past: React.FC = (): JSX.Element => {
  return (
    <OrderTemplate>
      <div>Past</div>
    </OrderTemplate>
  );
};

export default Past;
